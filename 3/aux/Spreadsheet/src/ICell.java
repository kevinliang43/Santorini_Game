//Cell interface to represent cells with values and empty cells
public interface ICell {

    //returns if the cell can be evaluated or not, if the cell has a formula or not
    boolean emptyCell();
}
